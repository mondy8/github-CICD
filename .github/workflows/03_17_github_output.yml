name: GITHUB_OUTPUT
on: push
jobs:
  share:
    runs-on: ubunut-latest
    steps:
      - id: source
        run: echo "result=HELLO" >> "${GITHUB_OUTPUT}"
      - env:
          RESULT: ${{ steps.source.outputs.result }}
        run: echo "${RESULT}"
